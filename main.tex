\documentclass[a4paper, 10pt]{article}

% Custom command parsing
\usepackage{xparse}



% Margins
\usepackage[margin=1in]{geometry}

% Font
\usepackage[T1]{fontenc}
\renewcommand*\familydefault{\sfdefault}
\usepackage{sfmath}

% Caption formatting:
\usepackage[labelfont=bf, textfont=normalfont, justification=raggedright, singlelinecheck=false, font=small]{caption}

% Title formatting
\usepackage{titlesec}
\titleformat{\section}
  {\normalfont\bfseries}
  {\thesection}{1em}{}
\titlespacing*{\section}{0pt}{1.2\baselineskip}{0.6\baselineskip}
\titleformat{\subsection}
  {\normalfont\itshape}
  {\thesubsection}{1em}{}
\titlespacing*{\subsection}{0pt}{1\baselineskip}{0.4\baselineskip}

% Columns
\usepackage{multicol}

% Maths
\usepackage{amsmath}
\usepackage{amssymb}

% Plotting
\usepackage{pgf}
\usepackage{graphicx}
\graphicspath{{figures/}}
\providecommand{\mathdefault}[1]{#1}

% Units
\usepackage{siunitx}

% Hyperlinks
\usepackage[colorlinks=true, linkcolor=blue, urlcolor=blue, citecolor=blue]{hyperref}

% Tables
\usepackage{booktabs}

% Layout
\usepackage{float}

% Headers and footers
\usepackage{fancyhdr}
\pagestyle{fancy}
\fancyhf{}                                  % clear all header/footer fields
\fancyhead[L]{Tahmid Azam}
\fancyhead[R]{Final Report (GG4)}
\fancyfoot[C]{\thepage}
\renewcommand{\headrulewidth}{0.4pt}        % rule between header and content
\renewcommand{\footrulewidth}{0.4pt}        % rule between content and footer

% Acrynoyms
\usepackage{acro}

% References
\usepackage[style=numeric,sortcites]{biblatex}
\addbibresource{references.bib}

% Render in-text citations as superscript numbers
\let\cite\supercite

\DeclareAcronym{bmi}{short=BMI,long=Brain--Machine Interface}
\DeclareAcronym{lgssm}{short=LGSSM,long=Linear Gaussian State--Space Model}
\DeclareAcronym{ols}{short=OLS,long=Ordinary Least Squares}
\DeclareAcronym{bic}{short=BIC,long=Bayesian Information Criterion}
\DeclareAcronym{cva}{short=CVA,long=Canonical Variate Analysis}
\DeclareAcronym{em}{short=EM,long=Expectation-Maximisation}
\DeclareAcronym{era}{short=ERA,long=Eigensystem Realisation Algorithm}
\DeclareAcronym{rms}{short=RMS,long=Root Mean Square}
\DeclareAcronym{lqr}{short=LQR,long=Linear Quadratic Regulator}
\DeclareAcronym{lqg}{short=LQG,long=Linear Quadratic Gaussian}
\DeclareAcronym{lqi}{short=LQI,long=Linear Quadratic Integral}
\DeclareAcronym{mpc}{short=MPC,long=Model Predictive Control}
\DeclareAcronym{tpe}{short=TPE,long=Tree-structured Parzen Estimator}
\DeclareAcronym{bo}{short=BO,long=Bayesian Optimisation}
\DeclareAcronym{anova}{short=ANOVA, long=Analysis of Variance}

% \pgffigure{<key>} inserts a float figure from figures/<key>.pgf, captions it
% from captions/<key>.tex, and labels it fig:<key>.
\NewDocumentCommand{\pgffigure}{s O{htbp} m}{%
  \IfBooleanTF{#1}{%
    \begin{figure*}[#2]%
      \centering%
      \input{figures/#3.pgf}%
      \caption{\protect\input{captions/#3.tex}}%
      \label{fig:#3}%
    \end{figure*}%
  }{%
    \begin{figure}[#2]%
      \centering%
      \input{figures/#3.pgf}%
      \caption{\protect\input{captions/#3.tex}}%
      \label{fig:#3}%
    \end{figure}%
  }%
}


% \latextable{<key>} inserts a float table from tables/<key>.tex, captions it
% from captions/<key>_table.tex, and labels it tab:<key>.
\NewDocumentCommand{\latextable}{s O{htbp} m}{%
  \IfBooleanTF{#1}{%
    \begin{table*}[#2]%
      \centering\small%
      \input{tables/#3.tex}%
      \caption{\protect\input{captions/#3_table.tex}}%
      \label{tab:#3}%
    \end{table*}%
  }{%
    \begin{table}[#2]%
      \centering%
      \resizebox{\columnwidth}{!}{\input{tables/#3.tex}}%
      \caption{\protect\input{captions/#3_table.tex}}%
      \label{tab:#3}%
    \end{table}%
  }%
}


% Automatic spacing
\usepackage{xspace}
\newcommand{\Brain}{\texttt{Brain}\xspace}
\newcommand{\LGSSM}{\ac{lgssm}\xspace}
\newcommand{\BMI}{\ac{bmi}\xspace}
\newcommand{\LQG}{\ac{lqg}\xspace}
\newcommand{\LQI}{\ac{lqi}\xspace}
\newcommand{\MPC}{\ac{mpc}\xspace}



\begin{document}
\thispagestyle{empty}                       % no header/footer on the title page
\noindent {\Large \textbf{Final Report}}

\vspace{0.5em}
\noindent Tahmid Azam\\
\noindent {\small Part IIA Project: \textit{Neural Control with Adaptive State Estimation}} (GG4)\\
\noindent {\small \today}

\vspace{1em}
\hrule
\vspace{1em}

\begin{abstract}
  We are provided a simulation of a prosthetic hand controlled through a fixed
  \BMI driven by a black-box \LGSSM \Brain, where only 16-dimensional \Brain
  observations and Cartesian hand position are available as feedback. We
  identify system matrices using \ac{era}+\ac{em} with latent dimension
  $n_x=4$, reconstructing the frequency response of both inputs where
  \ac{cva}+\ac{em} captures only the first. We demonstrate
  control over the population mean frequency spectrum, showing the strengths
  of \LQI over \LQG and \MPC in \ac{rms} error. We develop a pipeline to control
  the hand via 2 antagonistic muscle pairs which act on the shoulder and elbow
  joints that involves frequency band identification, gain matrix estimation,
  and a sequential damped strategy which mitigates issues of cross-talk. We
  achieve a final distance of $3.95 \pm 1.42$\,cm with a time to the
  \SI{10}{\centi\metre} threshold of $309 \pm 147$ steps, with a 100\% threshold reach
  rate. We find that the system's linearity permits a rhythm-based \BMI to be run
  open-loop.
\end{abstract}

\begin{multicols*}{2}
  \section*{Introduction}

  The system is an arm driven from neural signals, where the controller sees
  neither brain state nor muscle commands. A black box \LGSSM, irreducible
  measurement noise, and a fixed nonlinear \BMI decoder sitting in the loop
  provide challenges to control. This project explores how we can estimate
  system parameters and achieve spectral control. In addition, we compose a
  control strategy and explore its evaluation, sensitivity to its parameters,
  robustness to noise, limitations, and strengths.

  \section*{Problem statement}

  We are given a \Brain and a \ac{bmi}, which controls an arm. We are tasked
  with controlling the position of the hand at the end of the arm. The \Brain
  takes $n_u=2$ inputs and produces $n_y=16$ outputs; we assume it is a
  \ac{lgssm}. The \ac{bmi} takes the 16-dimensional observations from the \Brain
  and applies a fixed linear decoder to map them to 2 latent states: $x_1$, the
  muscle selection rhythm; and $x_2$, power rhythm. The \ac{bmi} stores a
  rolling history of $x_1$, which is passed through 4 fixed band-pass filters
  which measure how strongly $x_1$ oscillates at that muscle's defined
  excitation frequency. A history of $x_2$ is also stored in state, and the
  current value is compared to a half- and full-period in the past: if strong
  oscillation is found, evidence accumulates; otherwise, evidence decays,
  producing a smooth power state. Muscle activation is the product of the power state
  and the selection vector. Each muscle produces force proportional to its
  activation, and for each joint, the net force is the sum of signed muscle
  forces. Positive forces represent flexion; negative forces represent
  extension. A dry-friction threshold must first be overcome; once it is, the remaining net force (after subtracting the dry friction) is
  divided by wet friction representing viscous resistance. Joint angles are
  integrated, and 2-link arm kinematics give the hand position.

  \section*{Rules of engagement}

  The holistic view of this project is to be able to reason about and interpret a
  controller which takes observations from a brain (e.g. via electrodes on the
  scalp) to move a prosthetic arm. Critically, the brain is a black box and its
  model ``parameters'' cannot be directly observed. In the spirit of this view,
  we propose specific rules of engagement surrounding our analysis. We do not
  attempt to decompile the provided \Brain executables to derive the system
  matrices. We do not average over multiple \Brain seeds, removing noise to
  derive system matrices; we reserve this for evaluation purposes only to
  demonstrate robustness against noise. Where we do run experiments that are not
  physically realisable, we do this to demonstrate the direction of our
  analysis, and we replicate results in empirically valid ways alongside. We do
  not directly use the weights of the \BMI in our control efforts: we allow
  ourselves to read their values for experimental design, but we must prove any results empirically before use. We only allow the use of the 16-dimensional
  observations from the \Brain and the hand position given by the \BMI for
  closed-loop control; we do not rely on the internal state of the \BMI (e.g.,
  arm angles or latent state variables) for feedback. We also do not expect or
  attempt to control the elbow joint past \SI{180}{\degree} to mimic the
  capabilities of an anatomically accurate arm. We motivate these constraints as a way
  to achieve resilience and independence from both: inter-individual variation in
  brains (and therefore system matrices); and manufacturers of prosthetic arms
  (and therefore \BMI decoding, state, and arm kinematics defined by model
  weights). Finally, during control, the hand must be kept in the target
  position until the end of the trial.

  \section*{System identification}

  As we assume the \Brain is a \LGSSM, we explore system identification as a
  means to estimate the system matrices defining the model
  \cite{overscheeSubspaceIdentificationLinear1996}:
  \begin{align*}
    \underline{x}_{t+1} & = \underline{A}\,\underline{x}_t + \underline{B}\,\underline{u}_t + \underline{w}_t, & \underline{w}_t \sim \mathcal{N}(\underline{0}, \underline{Q}) \\
    \underline{y}_t     & = \underline{C}\,\underline{x}_t + \underline{o}_t,                                  & \underline{o}_t \sim \mathcal{N}(\underline{0}, \underline{R})
  \end{align*}
  Estimation here takes place in an online, causal context as we drive the
  \Brain with our own inputs.

  \subsection*{Identification evaluation}

  To evaluate our estimates, we have two strategies: the reconstruction of
  observations and the reconstruction of frequency response. To measure the
  reconstruction of observations, we use the coefficient of determination
  defined with the Frobenius norm:
  \begin{equation*}
    R^2 = 1 - \frac{\left\lVert Y - \hat{Y} \right\rVert_F^2}{\left\lVert Y - \overline{Y} \right\rVert_F^2}
  \end{equation*}
  This form is chosen for its effectiveness at aggregating errors across all
  time points and all observation dimensions. We weigh this up against its
  disadvantages, which include ignorance of observation covariance and temporal
  correlations. To measure the reconstruction of frequency response, we inspect
  a frequency map derived analytically from estimates of system matrices, and
  compare characteristics with empirical results from a single run of the
  \Brain. We find the transfer function of the \LGSSM, dependent on only the
  deterministic components (matrices $\underline{A}$, $\underline{B}$, and
  $\underline{C}$):
  \begin{equation*}
    \underline{H}(z) = \underline{C} \left(z\underline{I} - \underline{A}\right)^{-1}\underline{B}
  \end{equation*}
  We define an estimate to reconstruct the frequency response well if it can
  minimise the error between analytic and empirical values of
  $|H(\mathrm{e}^{\mathrm{j}\omega})|$ across all neurones and frequencies $[0,
        \pi]$. Given the emphasis on rhythm in the \BMI we weigh frequency response
  reconstruction more than observation reconstruction if a compromise is
  necessary.

  \subsection*{Identification methods}

  We run two \Brain instances with and without input to isolate and subtract the
  noise. We can then calculate the exact Markov parameters (impulse-response
  coefficients of the LGSSM $h_k =
    \underline{C}\underline{A}^{k-1}\underline{B}$), construct Hankel matrices
  (block matrix built by stacking time-shifted Markov parameters), and form a
  singular-value spectrum. With this analysis we find $\sigma_i / \sigma_1$ falls
  after $i=6$, indicating the true latent dimension number to be $n_x=6$
  (\autoref{fig:latent_dim}). However, this method is not physically realisable:
  you cannot run the same physiological brain twice with the same random state.
  This would mean rewinding a brain, re-running a trial without a stimulus, and
  subtracting: noise is truly stochastic outside of a simulation. A physically
  realisable single-run \ac{ols} regression (fitting system matrices by
  regressing each step on the previous one) estimate uncovers a noise floor that
  obscures modes 3--6. Balancing likelihood and model complexity with \ac{bic}
  model selection, we identify $n_x=2 \ne 6$ (\autoref{fig:latent_dim}).

  \pgffigure[H]{latent_dim}

  We plot a heatmap of $|H(\mathrm{e}^{\mathrm{j}\omega})|$ and find that
  \ac{cva}+\ac{em} reconstructs $u_1$ well, but not $u_2$, over all $n_x$
  candidates $\{2, 4, 6\}$ (\autoref{fig:freq_map} plots \ac{cva}+\ac{em} at
  $n_x=4$ only).
  \ac{cva} is a subspace method that defines the state as the linear combination
  of past data most correlated with future data. \ac{em} is an iterative
  maximum-likelihood estimator, where the E-step runs a Kalman smoother to infer
  states, and the M-step re-estimates the system matrices assuming them to be true
  \cite{shumwayApproachTimeSeries1982}. We turn to an estimation method that
  uses the \ac{era} (a method which derives deterministic system matrices
  $\underline{A}$, $\underline{B}$, and $\underline{C}$ directly from the
  singular-value spectrum of the Hankel matrix of Markov parameters
  \cite{juangEigensystemRealizationAlgorithm1985}) and \ac{em} that utilises
  \ac{ols}-estimated Markov parameters and find similar results for $n_x=2$:
  good reconstruction of $u_1$ but poor for $u_2$. However, in contrast to
  \ac{cva}+\ac{em}, reconstruction of the $u_2$ frequency response improved with
  $n_x=4$ and $n_x=6$ (\autoref{fig:freq_map}). More generally, we observe that
  the \Brain acts as a low-pass filter. The observation reconstruction quality
  remained similar between \ac{cva}+\ac{em} and \ac{era}+\ac{em}
  (\autoref{fig:obs_reconstruction}).

  As there was not a huge difference between $n_x=4$ and $n_x=6$, we select
  \ac{era}+\ac{em} and $n_x=4$, which yields system matrices visualised in
  \autoref{fig:id}.

  \pgffigure[H]{obs_reconstruction}
  \pgffigure[H]{id}
  \pgffigure*{freq_map}

  \section*{Closed-loop spectral control of brain observations}

  We test whether closed-loop stimulation can reproduce a prescribed frequency
  spectrum in neural population mean activity. We motivate the following
  evaluation framework and method with the emphasis on controlling rhythms in
  the \Brain observations for influence over \BMI latent variables and the arm
  downstream.

  \subsection*{Spectral control evaluation}

  We use the following metrics to evaluate each controller: spectral \ac{rms}
  error, which measures amplitude deviation at target frequencies; the maximum
  amplitude error, the worst-case per-component deviation; and the off-target
  power ratio, the mean spectral amplitude at non-target frequencies, relative
  to mean target amplitude.

  \subsection*{Spectral control method}

  We offer 3 candidates for control strategies: \ac{lqg} to respect the \Brain's
  \LGSSM structure; \ac{lqi} to correct steady-state error; and \ac{mpc} to
  optimise for and appreciate constraints on $u_i \in [0,1]$. \LQG involves a
  Kalman filter \cite{kalmanNewApproachLinear1960} feeding a \ac{lqr} feedback
  gain that aims to minimise a quadratic cost on state error and control effort,
  a controller specific to the \Brain's \LGSSM structure. We augment this with
  an integrator on the latent state tracking error, which attempts to drive
  steady-state error to 0. \MPC is a horizon-based controller that re-solves a
  constrained finite-horizon optimisation at each step, taking the optimal first
  value each time. This optimisation step allows it to respect constraints on
  inputs.

  \pgffigure*{spectral_control}

  \subsection*{Spectral control results}

  Inspecting the raw population mean traces in \autoref{fig:spectral_control},
  we find \LQG overestimates the population mean with a steady-state error,
  which is removed effectively by \LQI. \MPC fails to reconstruct the higher
  frequency components. We select \LQI due to its lower control effort compared
  to \LQG and better reconstruction performance over \MPC, shown in
  \autoref{fig:spectral_control_metrics}.

  \pgffigure[H]{spectral_control_metrics}

  \section*{Hand control}

  The \BMI's mechanism of muscle selection and a shared power state means
  that simultaneous control of different muscles is unlikely, as there is no
  granular control to direct different muscles at different power at the same
  time. This constrains our control strategy to be a sequential one, directing
  one muscle at a time.

  \subsection*{Inverse kinematics}

  Inverse kinematics is used to map Cartesian hand positions to shoulder and
  elbow angles. With equal link lengths $L=30$, the law of cosines fixes the
  elbow bend, and the isosceles geometry places the shoulder a half-bend below
  the bearing to the target:
  \begin{align*}
    \theta_e & = \arccos\frac{r^2-2L^2}{2L^2},                  \\
    \theta_s & = \operatorname{atan2}(y,x)-\tfrac{1}{2}\theta_e
  \end{align*}
  The sign of $\theta_e$ is the elbow-up/elbow-down redundancy; as only the hand
  position is observable, we resolve it toward the previous pose, keeping the
  inferred joint trajectory continuous and avoiding spurious elbow flips.
  Targets beyond the reachable disk ($r>2L$) are clipped to the nearest pose.

  \subsection*{Evaluating hand control}

  We aim to minimise the final distance to target, the Euclidean distance
  between the hand and the target at the end of a trial. We define a success
  threshold at 10. We aim to maximise the reach rate, the fraction of trials
  that reach within this success threshold. This metric captures reliability
  across diverse targets in the reachable area: a controller with low mean final
  distance but occasional failures is not practically useful for a
  prosthetic. We minimise the time to threshold, the number of steps until the
  hand first enters the distance success threshold: a useful prosthetic should
  converge in reasonable time to provide utility. We maximise the sector
  fraction, the proportion of time the hand spends in the optimal sector: an
  angular wedge containing the target and the start position. We minimise the
  rotational distance, the total angle swept by the hand trajectory over the
  trial. These two metrics penalise inefficient trajectories (e.g. looping
  behaviours), which would be inconvenient and unpredictable for a prosthetic.
  We minimise the control effort, the sum of squared brain inputs, in an effort
  to make inputs more interpretable and efficient.

  \subsection*{Muscle selection}

  We sweep frequencies for both inputs and measure shoulder and elbow angles,
  determined by inverse kinematics, to find the specific frequencies that excite
  specific muscles. We empirically recover the frequency bands defined in the
  \BMI weights using a single seed, shown in
  \autoref{tab:muscle_selection_freq_sweep} and
  \autoref{fig:muscle_selection_freq_sweep}.

  We observe that amplitude decreases with frequency, making it harder to drive
  muscles that require higher frequencies. This may be due to the low-pass
  action of the \Brain. We also observe cross-talk for elbow movements but not
  shoulder movements: the shoulder can be moved independently of the elbow, but
  the elbow cannot be moved independently of the shoulder.

  We then ask whether neural observations carry a muscle-specific spatial code:
  if different neurones respond preferentially at each selection frequency,
  the control target could be weighted to favour one muscle over another. For each
  selection frequency we record the full $n_y = 16$\,D amplitude spectrum and
  normalise the per-neurone profile by its own neurone-mean
  (\autoref{fig:neural_correlates}).

  \latextable[H]{muscle_selection_freq_sweep}
  \pgffigure[H]{muscle_selection_freq_sweep}
  \pgffigure[H]{neural_correlates}

  Muscle identity is therefore encoded temporally by the oscillation frequency
  of $x_1$, not spatially by which neurones are active (\autoref{fig:neural_correlates}).
  No differential weighting of neurones in the spectral control target offers
  muscle selectivity beyond what the \BMI's band-pass filters already provide.
  This justifies controlling the population mean at the spectral target.

  \subsection*{Control method}

  The core pipeline is a 3-step process. First is band-identification, an
  open-loop sinusoidal sweep over both brain input channels, with cumulative
  shoulder and elbow displacement (inferred via inverse kinematics) recorded per
  frequency. The four bands are identified, which correspond to S$+$, S$-$,
  E$+$, and E$-$. The next step is gain calibration, where a short closed-loop
  \LQI trial at each band frequency targets a sinusoidal spectral setpoint, and
  mean joint angle per unit amplitude forms a $2\times4$ gain matrix where rows
  are joints, and columns are bands. Cross-talk is re-identified here.

  \pgffigure[H]{gain_matrix}

  The third is the main control loop, where
  inverse kinematics converts Cartesian targets to joint-angle goals, band
  amplitudes are solved from the gain matrix, and an \LQI controller optionally
  closes the loop on the population mean.

  The elbow-to-shoulder cross-talk motivates a control strategy that first
  achieves the correct elbow angle, then reduces control gain over the elbow, and
  focuses on using S$+$ and S$-$ to correct the shoulder angle. In addition, during the
  initial correction angle phase, we should alternate between correcting the
  elbow angle and correcting the shoulder angle, as attempting to move the elbow
  moves the shoulder. To mitigate cross-talk further, we drive the shoulder in
  the opposite direction of the expected cross-talk.

  We always begin by targeting elbow error. Each elbow/shoulder phase
  has a minimum length and maximum length. In addition, when controlling the
  elbow, there is a maximum shoulder drift, such that if upon moving the elbow
  the shoulder drifts too much, we switch to controlling the shoulder. We also
  provide per-band gain multipliers applied to the gain matrix values before the
  solve, to further tune individual band aggressiveness. Once the elbow angle is
  below threshold, we begin damping the shoulder velocity in an effort to reduce
  oscillations.

  \subsection*{Hyperparameter optimisation}

  With so many model parameters defined by the complex sequential controller
  design, we turn to hyperparameter optimisation to search the parameter space
  for an optimal solution. We use \ac{tpe} via the \texttt{optuna} library
  \cite{akibaOptunaNextgenerationHyperparameter2019} to run two phases: first, a
  large set of \ac{bo} trials; then the top trials are re-evaluated on a full
  target grid, and the best is selected. \ac{bo} is effective as it samples
  where improvement is expected; its sample efficiency makes it more feasible than grid search for expensive trials.

  We define an objective score:
  \begin{equation*}
    \mathcal{S} = \overline{d}_{\mathrm{final}}
    - 20\,\overline{f}_{\mathrm{sector}}
    - 10\,\overline{p}_{\mathrm{reached}}
    + 50\,\overline{\max\!\left(0,\;\theta_{\mathrm{rot}} - 2\pi\right)}
  \end{equation*}
  This directly penalises final distance and inefficient trajectories, and rewards a successful reach rate to create a more interpretable optimisation process.
  \autoref{fig:cartesian_control_sweep} shows how the optimisation process did
  not reduce final distance or the time to threshold, but instead made the
  trajectory more sensible, increasing the sector fraction and reducing the
  rotation.

  \pgffigure[H]{cartesian_control_sweep}

  \subsection*{Results}

  The selected configuration and the range searched for each parameter are given
  in \autoref{tab:optimal_hyperparameters}; no optimum sits on a search bound.
  The optimal controller's metrics are summarised in
  \autoref{tab:cartesian_control_sweep}, its losses over time are plotted in
  \autoref{fig:cartesian_control_errors}, an example trajectory is illustrated
  in \autoref{fig:cartesian_control_trajectory}, and a heatmap of targets and
  the final distances to them is drawn in
  \autoref{fig:cartesian_control_heatmap}.

  The shoulder bands were scaled up and elbow bands were scaled down: the
  optimiser independently rediscovered the cross-talk hypothesis, that the elbow
  is ``loud'', driving the shoulder with it, so the controller converges on
  driving the elbow slowly. In addition, the minimum and maximum phase lengths
  are short, just a little larger than the state held by the \BMI, also
  rediscovering the hypothesis of small elbow movements that require quick and
  frequent shoulder corrections to minimise drastic cross-talk effects. The need
  for shoulder velocity damping is consolidated by the optimal controller
  converging on a high damping coefficient. The need for the elbow to be held
  once correct is also confirmed, due to the hard holding coefficient. Driving the shoulder in the opposite direction to minimise
  cross-talk effects did not seem valuable, likely due to the lack of
  granular control over selected muscle power. The most surprising result is
  that open-loop control wins over closed-loop control. We argue that the
  population-mean spectral target is only a loose proxy for the muscle selection
  latent state. In addition, as the \Brain is a linear system, sinusoidal inputs
  produce sinusoidal outputs so a \BMI based on rhythm can continue to operate
  without closed-loop control.

  \latextable[H]{optimal_hyperparameters}
  \latextable[H]{cartesian_control_sweep}
  \pgffigure[H]{cartesian_control_errors}
  \pgffigure*{cartesian_control_trajectory}
  \pgffigure[H]{cartesian_control_heatmap}

  \subsection*{Sensitivity to model parameters}

  We carry out ablation analysis, varying one parameter of the controller in
  isolation and holding everything else fixed to measure how sensitive
  performance is to that parameter. Hyperparameters under analysis include: the
  elbow band scale, how much the elbow gain is turned down relative to the
  shoulder gain (\autoref{fig:cartesian_control_ablation_band_scale}); the blend
  factor, how much we compensate for cross-talk by driving the shoulder in the
  opposite direction; closed-loop vs open-loop control
  (\autoref{fig:cartesian_control_ablation_control_mode}); and whether we
  constrain the elbow to a maximum of \SI{180}{\degree} or not
  (\autoref{fig:cartesian_control_ablation_elbow}). We filter the metrics using a
  one-way \ac{anova}, run independently for each metric across all conditions,
  with a null hypothesis that all group means are equal. A metric is plotted
  only if the \ac{anova} p-value satisfies $p \le 0.05$; cross-talk compensation
  produced no significant figures.

  \pgffigure[H]{cartesian_control_ablation_band_scale}
  \pgffigure[H]{cartesian_control_ablation_control_mode}
  \pgffigure[H]{cartesian_control_ablation_elbow}

  Only the effort significantly varied with the elbow band scale
  (\autoref{fig:cartesian_control_ablation_band_scale}). We find that open-loop
  control reduced the final distance, had less control effort, and slightly
  improved the sector fraction
  (\autoref{fig:cartesian_control_ablation_control_mode}). We find constraining
  the elbow to not extend past \SI{180}{\degree} worsens final distance,
  improves sector fraction, improves time-to-threshold, reduces effort, reduces
  rotation and improves the path. The constrained elbow is a much better
  controller in the areas reachable by an anatomically correct arm.
  However, in regions hard to access by such an arm (i.e., behind and towards
  the centre of the body, or negative-negative quadrant), its performance is
  poor, just like a real human. This is visualised in the heatmap in
  \autoref{fig:cartesian_control_constrained_heatmap}, where if the human were
  imagined to have their right shoulder joint at the origin, the poor final
  distances are behind the body, where a physiological elbow cannot reach.

  \pgffigure[H]{cartesian_control_constrained_heatmap}

  \subsection*{Robustness to noise}

  We evaluate control metrics after fitting to 1 seed only and applying it to 5
  and compare to re-fitting each seed before application in
  \autoref{fig:cartesian_control_pipeline_robustness}.



  We find that control metrics are highly seed-to-seed variable and perform
  poorly until they are re-fit.

  \section*{Alternative group approaches}

  One group member developed a control strategy with 2 levels of control,
  coarse and fine, with gains dynamically changing not when the elbow angle was
  correct, but rather when the hand arrived within a specific range of the target.
  Their preliminary results lacked the small final distances of our method, but
  had significantly faster time to threshold and lower control effort. Another
  group member had a model-free approach that our hyperparameter optimisation
  converged on; their preliminary results also had competitive metrics,
  showing potential that inspired the inclusion of the boolean flag to toggle
  open- vs. closed-loop control in the optimiser.

  \pgffigure[H]{cartesian_control_pipeline_robustness}

  \section*{Discussion}

  The strengths of our controller include: its reliability, a 100\% reach rate
  across the target grid; its principled interpretability, as the optimiser
  independently rediscovered intuitive strategies; an honest methodology where
  only 1 seed is sampled throughout keeping the controller physically
  realisable; and high trajectory quality with a high sector fraction, low
  rotation, and small path length.

  Limitations begin with a lack of evidence honestly recovering the true
  $n_x=6$, and the difficulty in reconstructing the frequency response of $u_2$
  to the same degree as $u_1$. The largest trade-off in the control strategy is
  controller interpretability, trajectory feasibility, and distance for the price
  of a poor time-to-threshold. This trade-off is established by the sequential
  focus on shoulder or elbow control, and limited attempts to control both
  joints at a time. In addition, the velocity damping to remove shoulder
  oscillations slows response time further. This decision was intuitively made
  on the basis of a shared power drive across selected muscles. Hyperparameter
  optimisation did not improve the headline final distance metric; it only made
  trajectories more feasible, highlighting this trade-off further. We also
  hand-pick the scoring methods and each component's weights. This trade-off is
  manageable, as it is more convenient to have a predictable, more accurate
  prosthetic that is slow over an erratic, trajectory-inefficient one.

  Several improvements follow from these limitations. The clearest target is the
  time-to-threshold penalty: driving both joints together, as in the
  coarse-to-fine scheme a group member trialled, would relax the
  one-joint-at-a-time constraint that the velocity damping compounds. Closing the
  loop on a signal that tracks the muscle-selection latent more directly than the
  population mean could recover the feedback benefit the spectral target failed
  to deliver, while a richer $u_2$ excitation would expose the modes the
  single-run estimate buries beneath the noise floor. The hand-tuned objective
  weights could also be learned from a held-out target distribution to remove a
  source of bias.

  \section*{Conclusion}

  We identified the \Brain as a 4-state \LGSSM with a single-run
  \ac{era}+\ac{em} pipeline, controlled its observation spectrum with an \LQI
  controller, and used this to drive a sequential, cross-talk-aware hand
  controller tuned by \ac{tpe}. The result reached every target on the grid
  within the success threshold, at a mean final distance under \SI{4}{cm}. The
  design was not imposed by hand: the optimiser recovered the elbow-to-shoulder
  cross-talk asymmetry on its own, down-weighting elbow drive sixfold. The
  clearest result against expectation was that open-loop feedforward outperformed
  closing the loop on the population mean, which we attribute to that mean being
  a loose proxy for the muscle-selection latent and to the \Brain's linearity
  preserving rhythm without feedback. This accuracy and trajectory feasibility
  came at the cost of time-to-threshold, a direct consequence of driving one
  joint at a time.

  \section*{Acknowledgements}

  Thanks to Dr Flavia Mancini for leading the project, Qingyun Chen for
  demonstrating the project, and my group peers Freddie McElwaine-Johnn, Jerry
  Liu, and Asmee Mishra for their collaboration.

  \section*{Code availability}

  Source code for analysis, plotting, and report typesetting can be found at:
  \href{www.github.com/tahmidazam/gg4}{www.github.com/tahmidazam/gg4}.

  \printbibheading
  \renewcommand{\bibfont}{\footnotesize}
  \printbibliography[heading=none]
\end{multicols*}

\end{document}